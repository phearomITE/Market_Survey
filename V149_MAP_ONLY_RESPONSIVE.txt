V149 MAP-ONLY RESPONSIVE VIEWER

Changes
- Adds the missing app/web/map.html and app/web/map.css assets.
- Replaces the previous mixed map/dashboard JavaScript with a map-only viewer.
- Removes the Outlet List and Dashboard from the web UI and Telegram commands.
- Adds responsive desktop, tablet, phone, and Telegram WebView layouts.
- Uses Leaflet canvas circles with visible 1-10 scores for fast pan and zoom.
- Disables expensive mobile animations and bounds the mobile marker payload.
- Keeps filters, layers, satellite/street basemaps, legend, outlet details,
  product ratings, key issue, and direct Google Maps navigation.
- Adds GZip compression for map API responses.
- Allows every real report date stored in PostgreSQL to be selected.

Railway variables
PUBLIC_APP_URL=https://YOUR-SERVICE.up.railway.app
MAP_PUBLIC_VIEW_ENABLED=true
MAP_VIEWER_TOKEN=YOUR_LONG_RANDOM_VIEWER_TOKEN

Open
https://YOUR-SERVICE.up.railway.app/map?access=YOUR_LONG_RANDOM_VIEWER_TOKEN

Telegram
/map
