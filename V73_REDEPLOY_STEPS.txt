KB Market Survey V73 - Summary Colors, Detail 0-8, Excel Table Fix

CHANGES
1. Summary color layout matches the approved screenshot:
   - <5: purple #60497A
   - 5 to 8: dark red #963634
   - 9 to 10: green #00B050
   - Status remains blue
2. Detail sheet now includes CB LITE NCP movement scores from 0 through 8.
3. Detail header changed from <5 to 0 to 8.
4. Detail dealers are selected when final CB LITE NCP movement is 0 through 8.
5. The invalid template table is removed and rebuilt with valid sequential
   table-column IDs, preventing Excel's repair warning.
6. Google Maps links and competitor-lead fields remain unchanged.

GIT BASH
cd /d/Bot/Market_Survey_Git

unzip -o /c/Users/User/Downloads/KB_Market_Survey_V73_SUMMARY_COLOR_DETAIL_FIX.zip -d .

python -m py_compile app/reports/summary_report.py app/services/report_service.py

python -m pytest -q tests/test_v73_summary_color_detail_table.py

git add app/reports/summary_report.py app/services/report_service.py tests/test_v73_summary_color_detail_table.py

git commit -m "Fix summary colors detail range and Excel table"

git push origin main

After Railway is Active:
/summary 2026-07-25

Download a newly generated workbook. Old repaired workbooks remain damaged and
cannot be corrected automatically.
