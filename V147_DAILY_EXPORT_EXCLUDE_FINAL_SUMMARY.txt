KB Market Survey V147 - Clean Daily Export
===========================================

For /export YYYY-MM-DD:

1. Location_Outlet excludes final-summary submissions such as
   "បូកសរុបរួម". These control rows are not real outlets and will no longer
   appear with blank GPS, blank outlet type, and N/A phone.
2. Summary_Data retains the V146 Member rule: one most-frequent Member value
   per Region + Dealer. If frequencies tie, the lower number is used.

Apply and deploy
----------------
cd /d/Bot/Market_Survey_Git

unzip -o \
  /c/Users/User/Downloads/KB_Market_Survey_V147_DAILY_EXPORT_EXCLUDE_FINAL_SUMMARY.zip \
  -d .

python -m py_compile app/reports/movement_exports.py
python -m unittest tests.test_v104_daily_exports -v

git add \
  app/reports/movement_exports.py \
  tests/test_v104_daily_exports.py \
  V147_DAILY_EXPORT_EXCLUDE_FINAL_SUMMARY.txt

git commit -m "Exclude final summary rows from daily export"
git push origin main

After Railway becomes Active, test:
/export 2026-08-01
