V103 HEADLESS LIBREOFFICE PNG FIX

Confirmed Railway error:
soffice.bin X11 error: Can't open display

Cause:
SAL_USE_VCLPLUGIN=gen forced LibreOffice to use its X11 graphical backend.
Railway containers do not provide an X display.

Fix:
- Use SAL_USE_VCLPLUGIN=svp, LibreOffice's virtual headless backend.
- Verify soffice + svp during the Docker build.
- Preserve V101 combined FastAPI map/dashboard and Telegram launcher.
- Preserve Khmer OS fonts and km_KH.UTF-8 locale.

Railway must rebuild the Docker image after git push.
