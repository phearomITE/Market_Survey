V129 Ten-Second Excel Report Path

Cause of the remaining delay:
The single-dealer report loaded the large dynamic Kobo wide table even though
get_submissions had already eagerly loaded normalized product, competitor and
ring-pull metrics. Railway spent roughly 20-30 seconds in that unnecessary read.

Fix:
- Single /report uses its already-loaded normalized metrics.
- Skip the wide-table query only for the single-dealer report path.
- Keep exact lowercase Kobo dealer sync and stale-row refresh.
- Excel remains sent before PNG, so PNG never delays Excel delivery.

Run in Git Bash:

cd /d/Bot/Market_Survey_Git
unzip -o /c/Users/User/Downloads/KB_Market_Survey_V129_TEN_SECOND_EXCEL_REPORT_PATH.zip -d .

python -m py_compile app/services/report_service.py app/kobo/client.py app/kobo/sync.py
python -m unittest tests.test_v120_sync_and_khmer_png tests.test_v125_fast_reports_no_web -v

git status
git add app/kobo/client.py app/services/report_service.py tests/test_v120_sync_and_khmer_png.py V129_TEN_SECOND_EXCEL_REPORT_PATH.txt
git commit -m "Speed up single dealer Excel reports"
git push origin main

After Railway restarts, test one command at a time:
/report CA7 GT 2026-08-01
