V121 Railway Khmer Font Build Fix

Cause:
Debian Bookworm cannot locate the package fonts-khmeros-core, so Railway stops
during the Docker image build.

Fix:
- Remove unavailable fonts-khmeros-core.
- Keep fonts-noto-core and fonts-noto-extra for Noto Sans Khmer rendering.
- No application, report, product, Kobo form, or template changes.

Run in Git Bash:

cd /d/Bot/Market_Survey_Git

unzip -o \
  /c/Users/User/Downloads/KB_Market_Survey_V121_RAILWAY_KHMER_FONT_BUILD_FIX.zip \
  -d .

grep -n "fonts-" Dockerfile

git diff -- Dockerfile
git status

git add Dockerfile V121_RAILWAY_KHMER_FONT_BUILD_FIX.txt
git commit -m "Fix Khmer font package on Railway"
git push origin main

After deployment test:
/report CA8 GT 2026-08-01
/alert_submit 10
/alert_submit 20
