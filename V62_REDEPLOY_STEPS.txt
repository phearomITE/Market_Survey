KB Market Survey V62 - Product Outlet Counts + Consistent Movement

UPDATED FEATURES
1. /export uses the approved Template_Data_Survey.xlsx header order.
2. Product-specific columns:
   WS = Wholesale outlets selling the product
   DS = Drink Shop outlets selling the product
   WM = Wet Market outlets selling the product
   TL = Trolley outlets selling the product
   LE = Local Eat outlets selling the product
   CB = Coffee,Bakery outlets selling the product
   MS = Motor Shop outlets selling the product
3. Dealer-level outlet totals remain in Wholesale, Drink Shop, Wet Market,
   Trolley, Local Eat, Coffe,Bakery, Canteen, Sport Club and Motor Shop.
4. Export Movement uses GENERAL outlet types only, so it matches the final
   General Market Improvement Report and /summary.
5. /summary uses the same final normalized CB LITE NCP and competitor movement.
6. Wide Kobo rows are batch-loaded once to reduce /summary and /export time.
7. /export command and missing data_export.py are included.

GIT BASH
cd /d/Bot/Market_Survey_Git

unzip -o /c/Users/User/Downloads/KB_Market_Survey_V62_PRODUCT_COUNTS_MOVEMENT.zip -d .

python -m py_compile \
  app/reports/aggregator.py \
  app/reports/data_export.py \
  app/reports/member_mode.py \
  app/reports/summary_report.py \
  app/services/report_service.py \
  app/bot/handlers.py \
  app/bot/run_bot.py

python -c "from app.reports.data_export import create_data_export; print('EXPORT IMPORT OK')"

git add \
  app/bot/handlers.py \
  app/bot/run_bot.py \
  app/reports/aggregator.py \
  app/reports/data_export.py \
  app/reports/member_mode.py \
  app/reports/summary_report.py \
  app/services/report_service.py \
  templates/Template_Data_Survey.xlsx \
  tests/test_v62_export_product_counts.py

git commit -m "Add product outlet counts and align movement calculations"
git push origin main

TEST AFTER RAILWAY IS ACTIVE
/summary 2026-07-18
/export 2026-07-18
