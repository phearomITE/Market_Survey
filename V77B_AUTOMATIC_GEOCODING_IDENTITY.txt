KB Market Survey V77B - Automatic Geocoding Identity
=====================================================

Removes the REVERSE_GEOCODING_USER_AGENT Railway variable.
The application now builds its HTTP identity automatically from PUBLIC_APP_URL:

KBMarketSurvey/1.0 (+https://marketsurvey-production.up.railway.app)

Keep only:
REVERSE_GEOCODING_ENABLED=true
REVERSE_GEOCODING_BATCH_SIZE=50

Deploy:
python -m py_compile app/core/config.py app/web/geocode.py
git add app/core/config.py app/web/geocode.py V77B_AUTOMATIC_GEOCODING_IDENTITY.txt
git commit -m "Derive geocoding identity from public app URL"
git push origin main
