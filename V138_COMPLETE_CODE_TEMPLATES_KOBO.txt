V138 complete code, templates, and Kobo form

Why the previous update was not visible:
- The old full-project ZIP contained an extra Market_Survey/ directory.
- Extracting it inside D:/Bot/Market_Survey_Git created a nested folder instead of replacing the active files.

V138 fix:
- This ZIP is drop-in: app/, templates/, tests/, Dockerfile, and project files are at the ZIP root.
- Both Kobo filenames contain the same current GT/HORECA conditional form.
- template_by_dealer.xlsx is synchronized with template_general.xlsx.
- template_horeca.xlsx is synchronized with template_horeca_products.xlsx.
- The default TEMPLATE_PATH is templates/template_general.xlsx.
- Region, Dealer, and Report Type appear first in Kobo.
- Outlet details and products appear only after GT or HORECA is selected.
- GT and HORECA product routing, CB LITE NCP, final movement, fast Kobo fetch, Excel report, and PNG preview fixes are included.

Run in Git Bash after downloading:

cd /d/Bot/Market_Survey_Git

unzip -o \
  /c/Users/User/Downloads/Market_Survey_V138_COMPLETE_DROP_IN_READY.zip \
  -d .

python -m py_compile \
  app/core/config.py \
  app/kobo/client.py \
  app/kobo/parser.py \
  app/kobo/sync.py \
  app/reports/aggregator.py \
  app/reports/excel_report.py \
  app/reports/movement_exports.py \
  app/reports/summary_report.py \
  app/services/render_service.py \
  app/services/report_service.py \
  app/services/submission_alert_service.py \
  app/bot/handlers.py \
  app/bot/run_bot.py \
  app/main.py

python -m unittest \
  tests.test_v104_daily_exports \
  tests.test_v105_business_movement \
  tests.test_v135_authoritative_kobo_features \
  tests.test_v136_form_horeca_region_png \
  tests.test_v137_kobo_report_type_visibility \
  tests.test_v138_complete_template_bundle -v

sha256sum \
  templates/KB_Market_Improvement_XLSForm.xlsx \
  templates/KB_Market_Improvement_XLSForm_GT_HORECA.xlsx

sha256sum \
  templates/template_by_dealer.xlsx \
  templates/template_general.xlsx

sha256sum \
  templates/template_horeca.xlsx \
  templates/template_horeca_products.xlsx

git status --short

git add \
  Dockerfile \
  railway.json \
  requirements.txt \
  .env.example \
  app \
  templates \
  tests \
  V136_KOBO_GT_HORECA_REGION_PNG_FIX.txt \
  V137_KOBO_REPORT_TYPE_VISIBILITY.txt \
  V138_COMPLETE_CODE_TEMPLATES_KOBO.txt

git commit -m "Install complete GT HORECA code and templates"

git push origin main

After Railway deployment:
1. In Kobo, replace/redeploy the form using:
   templates/KB_Market_Improvement_XLSForm_GT_HORECA.xlsx
2. Test the blank start screen, then GT, then HORECA.
3. Test /report DEALER GT YYYY-MM-DD and /report DEALER HORECA YYYY-MM-DD.

The paired sha256sum commands must show matching hashes. That proves the old filenames were replaced with the current templates.
