V101 final deployment

Fixes:
- Correct Khmer shaping in PNG using Khmer OS Battambang and km_KH locale.
- Start FastAPI map/dashboard and Telegram polling in one asyncio lifecycle.
- Repair map 500 error by adding province/district/commune/village fields.
- Show and edit ស្ថានភាពលក់: អត់មានលក់ / មានលក់ / លក់ដាច់.
- Add /health endpoint.

Railway required variables:
TELEGRAM_BOT_TOKEN
DATABASE_URL
KOBO_TOKEN
KOBO_ASSET_UID
PUBLIC_APP_URL
MAP_VIEWER_TOKEN
MAP_EDITOR_TOKEN (optional; required only for editing)

Deploy from Git Bash:

cd /d/Bot/Market_Survey_Git

python -m py_compile \
  app/launcher.py \
  app/bot/run_bot.py \
  app/main.py \
  app/web/router.py

git status

git add Dockerfile railway.json app tests V101_KHMER_MAP_SALES_STATUS_DEPLOY.txt
git commit -m "Fix Khmer PNG map data and sales status"
git push origin main
